\documentclass[11pt]{article}

% --- Layout: kompakt, für ca. 1 Seite Stichpunkte ---
\usepackage[a4paper,margin=2cm]{geometry}
\usepackage[ngerman]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{csquotes}
\usepackage{graphicx}
\hypersetup{colorlinks=true, linkcolor=black, urlcolor=blue}

\setlist{nosep, leftmargin=1.4em, itemsep=1pt, topsep=2pt}
\titlespacing*{\section}{0pt}{8pt}{3pt}
\titlespacing*{\subsection}{0pt}{5pt}{2pt}
\titleformat{\section}{\normalfont\bfseries\large}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalfont\bfseries}{\thesubsection}{0.6em}{}

\pagenumbering{gobble} 

\begin{document}

\begin{center}
    {\Large\bfseries Notizen --- Vorab-Aufgabe Datenkonsolidierung}\\[2pt]
    \small [Kristian] \hfill [16.08.2026]
\end{center}
\vspace{4pt}
% =========================================================
\section{Prioritäten}
\begin{enumerate}
    \item Zuverlässiges, robustes und reproduzierbares Vorgehen zur Extraktion und Migration der notwendigen Informationen.
    \item Inkrementelles Erweitern des Outputs der Felder um sich vorläufigem Ziel-Schema anzunähern. Grundlage hierfür eine zukunftsfähige und wartbare Lösung für kommerziellen Gebrauch.
    \item Erarbeiten besonderer Herausforderungen der Datenkonsolidierung (z.B. fehlende Informationen, Inkonsistenzen, Abweichungen zwischen Standorten).
    \item Transparente Dokumentation der Entscheidungen und Vorgehensweise, um die Nachvollziehbarkeit zu gewährleisten. Auflistung weiterer Potenziale und aktueller Leistungen.
\end{enumerate}

% =========================================================
\section{Vorgehen: Ziel I}
\subsection{Exploration}
\begin{itemize}
    \item (I) Prüfen der Intra-Standort Variabilität der Eingaben (i.e. wie hoch ist die Eingabekonsistenz innerhalb der Standorte)
    \item (I) Prüfen der Inter-Standort Variabilität der Eingaben (i.e. wie hoch ist die Eingabekonsistenz zwischen den Standorten)
    \item Ziel: Gibt es Gemeinsamkeiten zwischen den Standorten die die eine eineitliche Extraktion von Informationen zulassen (\"required\")?
    \item Ziel: Welche Abweichungen erschweren die Extraktion von Informationen?
    \item Ergebnis: Folgende Tabelle zeigt welche Informationen einheitlich aus allen Dateien zuverlässig ausgelesen werden können:
\end{itemize}
\begin{table}[h]
    \centering
    \begin{tabular}{@{} p{0.52\linewidth} p{0.38\linewidth} @{}}
        \toprule
        \textbf{.html Quelle} & \textbf{Info} \\
        \midrule
        Titel (<h1> Tag) & Zimmer, Vermarktungsart, Objekttyp \\
        <div class=\enquote{expose} data-objekt-nr=\enquote{xxxx}> & Objekt-ID \\
        Objekt-Pfad & Quelle \\
        <p class=\enquote{lage}>\enquote{xxxx}</p> & Adresse\\
        <table class=\enquote{eckdaten}> & Wohnflaeche\\
        \bottomrule
    \end{tabular}
\end{table}
\subsection{Umsetzung der Erkenntisse}
\begin{itemize}
    \item Regex und Wortsuche aus auf <h1> als Primärquelle mit Fallback auf Eckdaten Tabelle. Warnungen und Errors werden geloggt und gesammelt mit einer Fehlerbeschreibung.
    \item Aufgrund von nicht eideutig lösbaren Inkonsistenzen erwarten die fehlerhaften Outputs einen HITL Ansatz.
\end{itemize}
\section{Vorgehen: Ziel II}
\begin{itemize}
    \item (II) Erwägen welches Datenmodell für den Kunden zukünftig am besten geeignet ist.
    \item Ziel: Vorschlag einer SQL-Datenbank für kommerziellen Gebrauch.
    \item Ergebnis: Erweitern des aktuellen Schemas um Informationen die für KundInnen und Vertrieb in einer SQL-Datenbank relevant sind
\end{itemize}
% =========================================================
\section{Nutzung KI}
\begin{itemize}
    \item (1)Initiale Vorschläge der KI berücksichtigten nicht wie Fehler im Output behandelt wurden und entschied eigenständig.
    \item Änderung (1): Transparenz durch seperate Ablage und Flagging von fehlerhaften Extraktionsprozessen (Abbildung \ref{fig:error_1}, Abbildung \ref{fig:error_2})
    \item (2) KI schlägt Validierung des Outputs durch \enquote{jsonschema} vor.
    \item Änderung (2): Validierung des Outputs durch \enquote{jsonschema} alleine könnte zu starr sein, da die genau Ursache von Abweichungen in Größe und Art unklar bleibt. Stattdessen wird der Output in eine separate Datei geschrieben und mit einem Flag versehen, um die fehlgeschlagene Validierung zu kennzeichnen (\ref{fig:valid}).
\end{itemize}

\input{figures/error_1.tex}
\input{figures/error_2.tex}
\input{figures/valid.tex}

% =========================================================


% =========================================================


% =========================================================


\end{document}

% ============================================================
% Review: extract_required.py
% ============================================================
%
% --- Staerken ---------------------------------------------
% - Titel (<h1>) als Primaerquelle statt "erste gefundene Zahl":
%   ueber alle 50 Dateien 0 Abweichungen zw. Titel und Tabelle/Freitext
% - Bewusster Ausschluss des Stoersatzes ("Die X Zimmer verteilen
%   sich auf zwei Ebenen") -- erkennt Fallen im Text statt naiv
%   zu parsen
% - Konflikte werden geloggt statt still ueberschrieben
%   (Zimmerzahl-Konflikt: Titel=..., weitere Quellen=...)
% - Sichtbare Trennung valide/fraglich: ergebnis/ vs.
%   ergebnis/zu_pruefen/, plus _report.json und Exit-Code != 0
% - Deterministisch und kostenlos, kein LLM noetig fuer diese
%   7 Felder, reproduzierbar bei jedem Lauf
% - Gegen vollstaendigen Datensatz getestet (50/50), nicht nur
%   Stichprobe -- "46 sauber / 4 mit bekannter Luecke" ist belastbar
%
% --- Schwaechen ----------------------------------------------
% - Kein Freitext-Fallback fuer wohnflaeche_qm: 4/50 Objekte mit
%   null in Pflichtfeld -> landen in zu_pruefen/, aber ungeloest
% - Keine Pruefung auf doppelte objekt_id: zweites Objekt mit
%   gleicher ID ueberschreibt Datei still, ohne Warnung
% - _dezimal() fuer Flaeche UND Zimmer genutzt, ohne Bereichspruefung
%   gegen Schema-Grenzen (zimmer max 50, flaeche max 5000)
% - Keine echte Validierung gegen immobilien_schema.json --
%   fehlende_pflichtfelder() ist von Hand parallel gepflegte Kopie,
%   divergiert still bei Schema-Aenderung
% - Adressparsing ist eine einzelne monolithische Regex: ein
%   unerwartetes Format (z.B. keine Hausnummer) wirft das
%   gesamte Feld, statt Teilwerte zu retten
% - Keine versionierten Testfaelle -- "50/50 getestet" beruht auf
%   manuellem Skriptlauf, Regressionen faellen ggf. nicht auf
%
% --- Annahmen der Pipeline ------------------------------------
% 1. <h1>-Titel folgt immer exakt "<N>-Zimmer-<Typ> in <Ort>
%    zu <verkaufen|vermieten>"
% 2. Titel ist inhaltlich immer korrekt (Anker ohne unabhaengige
%    Gegenpruefung)
% 3. Objekttypen im Titel: nur Wohnung/Haus/Gewerbeflaeche
%    (+ ungetestet Grundstueck) -- Rest faellt auf 'sonstige'
% 4. Adresse folgt immer "Strasse Hausnummer, PLZ Ort (Stadtteil)",
%    genau eine 5-stellige PLZ, keine Postfaecher/Auslandsadressen
% 5. Flaechen-/Zimmerangaben nie mit Tausendertrenner
%    (nur Dezimalkomma)
% 6. "ca." ist das einzige Rauschwort vor Zahlenwerten
% 7. Wohnflaeche steht (fast) immer in der Eckdaten-Tabelle,
%    Freitext-Fall ist Ausnahme, nicht Regelfall
% 8. Genau ein div.expose, genau eine Eckdaten-Tabelle, genau
%    ein <h1> pro Datei
% 9. Alle Dateien direkt unter standort_*/*.html, UTF-8,
%    mit html.parser korrekt parsbar
% 10. data-objekt-nr immer vorhanden, eindeutig, stabil ueber
%     Zeit -- keine Kollisionen zwischen/innerhalb Standorten